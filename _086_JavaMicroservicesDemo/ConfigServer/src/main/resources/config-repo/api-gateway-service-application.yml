server:
  port: 80

spring:
  cloud:
    gateway:
      default-filters:
        # http://localhost:9090/auth/************************
        - RewritePath= /(?<segment>.*), /$\{segment}
      routes:
        - id: auth-service
          uri: http://localhost:9090
          predicates:
            - Path=/auth/**

